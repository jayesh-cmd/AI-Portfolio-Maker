<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ Name }} | Portfolio</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(120deg, #222831 0%, #323744 100%);
            color: #e0e6ed;
        }
        .container {
            display: flex;
            min-height: 100vh;
        }
        .sidebar {
            width: 220px;
            background: linear-gradient(120deg, #232526 0%, #414345 100%);
            color: #fff;
            padding: 40px 0 0 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-shadow: 2px 0 8px #181a1b;
        }
        .sidebar nav {
            display: flex;
            flex-direction: column;
            width: 100%;
            gap: 8px;
            margin-top: 50px;
        }
        .sidebar a {
            text-decoration: none;
            color: #e0e6ed;
            padding: 12px 28px;
            border-radius: 8px;
            transition: background 0.2s;
            font-size: 1.04rem;
            font-weight: 500;
            letter-spacing: 0.5px;
            text-align: left;
        }
        .sidebar a:hover, .sidebar a.active {
            background: #393e46;
        }
        .main {
            flex: 1;
            padding: 30px 36px 36px 36px;
            display: flex;
            flex-direction: column;
        }
        .portfolio-header {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 30px;
        }
        .home-title {
            font-size: 1.5rem;
            font-weight: 600;
            letter-spacing: 2px;
            color: #e0e6ed;
            margin-top: 24px;
        }
        .portfolio-subtitle {
            font-size: 0.98rem;
            color: #b0b3b8;
            margin-top: 3px;
            letter-spacing: 2px;
        }
        .section {
            margin-bottom: 34px;
            display: none;
            animation: fadeIn 0.5s;
        }
        .section.active {
            display: block;
        }
        @keyframes fadeIn {
            from {opacity: 0;}
            to {opacity: 1;}
        }
        .section-title {
            font-size: 1.14rem;
            color: #8ecae6;
            margin-bottom: 17px;
            letter-spacing: 1px;
        }
        .education-list, .skills-list, .projects-list {
            padding-left: 0;
            list-style: none;
        }
        .education-item {
            background: #292d36;
            margin-bottom: 13px;
            padding: 13px 16px;
            border-radius: 7px;
            box-shadow: 0 2px 8px #181a1b40;
        }
        .education-item .degree {
            font-size: 1.06rem;
            font-weight: 500;
            color: #fff;
        }
        .education-item .institute {
            font-size: 0.97rem;
            color: #a5a7b0;
            margin-bottom: 2px;
        }
        .education-item .years {
            font-size: 0.9rem;
            color: #969ba0;
        }
        .skills-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .skill {
            background: #35363a;
            color: #d1d1d1;
            padding: 7px 17px;
            border-radius: 7px;
            font-size: 0.94rem;
            letter-spacing: 0.5px;
            box-shadow: 0 1px 3px #1a1a1a55;
        }
        .projects-list {
            display: flex;
            flex-direction: column;
            gap: 19px;
        }
        .project-item {
            background: #282b2f;
            padding: 14px 18px;
            border-radius: 8px;
            box-shadow: 0 2px 8px #23252855;
        }
        .project-title {
            font-size: 1.12rem;
            font-weight: 600;
            color: #90caf9;
            margin-bottom: 2px;
        }
        .project-desc {
            font-size: 0.97rem;
            color: #bac8d3;
            margin-bottom: 6px;
        }
        .project-link a {
            color: #8ecae6;
            text-decoration: underline dotted;
            font-size: 0.98rem;
        }
        .contact-section {
            font-size: 1.07rem;
            color: #bac8d3;
        }
        .contact-info {
            margin-bottom: 7px;
            color: #b0b3b8;
        }
        @media (max-width: 700px) {
            .container { flex-direction: column; }
            .sidebar { width: 100%; flex-direction: row; justify-content: center; box-shadow: none; }
            .sidebar nav { flex-direction: row; gap: 0; }
            .main { padding: 15px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <aside class="sidebar">
            <nav>
                <a href="#about" class="active" onclick="showSection('about', this)">About Me</a>
                <a href="#skills" onclick="showSection('skills', this)">Skills</a>
                <a href="#projects" onclick="showSection('projects', this)">Projects</a>
                <a href="#contact" onclick="showSection('contact', this)">Contact Me</a>
            </nav>
        </aside>
        <main class="main">
            <div class="portfolio-header">
                <div class="home-title">{{ Name }}</div>
                <div class="portfolio-subtitle">a portfolio site</div>
            </div>
            <section id="about" class="section active">
                <div class="section-title">About Me</div>
                <div class="education-list">
                    {% for edu in Education %}
                        <div class="education-item">
                            <div class="degree">{{ edu.degree }}</div>
                            <div class="institute">{{ edu.institute }}</div>
                            <div class="years">{{ edu.years }}</div>
                        </div>
                    {% endfor %}
                </div>
            </section>
            <section id="skills" class="section">
                <div class="section-title">Skills</div>
                <div class="skills-list">
                    {% for skill in Skills %}
                        <span class="skill">{{ skill }}</span>
                    {% endfor %}
                </div>
            </section>
            <section id="projects" class="section">
                <div class="section-title">Projects</div>
                <div class="projects-list">
                    {% for proj in Projects %}
                        <div class="project-item">
                            <div class="project-title">{{ proj.title }}</div>
                            {% if proj.description %}
                                <div class="project-desc">{{ proj.description }}</div>
                            {% endif %}
                            <div class="project-link">
                                <a href="{{ proj.link }}" target="_blank">GitHub Repo</a>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </section>
            <section id="contact" class="section contact-section">
                <div class="section-title">Contact Me</div>
                <div class="contact-info">
                    <b>Email:</b> {{ Email }}
                </div>
                <div class="contact-info">
                    <b>Phone:</b> {{ Phone }}
                </div>
            </section>
        </main>
    </div>
    <script>
        // Simple JS to show only one section at a time
        function showSection(sectionId, navElem) {
            // Remove 'active' class from all nav links
            var navLinks = document.querySelectorAll('.sidebar a');
            navLinks.forEach(function(link) { link.classList.remove('active'); });
            // Add 'active' class to clicked nav link
            navElem.classList.add('active');
            // Hide all sections
            var sections = document.querySelectorAll('.section');
            sections.forEach(function(section) { section.classList.remove('active'); });
            // Show selected section
            document.getElementById(sectionId).classList.add('active');
        }
    </script>
</body>
</html>
